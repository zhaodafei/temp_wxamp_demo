<view class="my-page">

  <view class="container">
    <view>
      <button type="primary" class="btn" bindtap="inputCarNum">输入车牌号</button>
    </view>

    <block wx:if="{{showKeyboard}}">
      <VehicleKeyboard2 bind:onOk="onOk" bind:onCancel="onCancel"/>
    </block>
  </view>
</view>
